#include <bits/stdc++.h>
#include "template.h"

using namespace std;
using namespace sjtu;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // The OJ harness links its own tests against our header.
    // Provide a minimal main that does nothing.
    // To allow local ad-hoc testing if needed, we could parse commands,
    // but problem statement does not require any I/O behavior here.
    return 0;
}

